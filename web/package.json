{
  "name": "ssh-chess-web",
  "version": "1.0.0",
  "description": "A web app that opens a terminal and connects to ssh-chess",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@xterm/xterm": "^5.5.0"
  }
}
